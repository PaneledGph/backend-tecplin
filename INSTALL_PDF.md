# Instalación de dependencias para PDF

## Instalar pdfkit para generar PDFs

```bash
cd c:\ie\proyectos\backend-tecplin
npm install pdfkit
npm install @types/pdfkit --save-dev
```

## Uso
El servicio de PDF se creará en `src/ordenes/pdf.service.ts`
