# Instalación de Multer para subir archivos

## Instalar multer y tipos

```bash
cd c:\ie\proyectos\backend-tecplin
npm install multer
npm install @types/multer --save-dev
npm install @nestjs/platform-express
```

## Uso
Multer permite recibir archivos desde el frontend (fotos, PDFs, etc.)
